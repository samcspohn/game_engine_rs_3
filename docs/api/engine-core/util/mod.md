# engine-core::util::mod

Generated by `make api` — signatures only. Read the source of the
one item you are changing, not the module to find out what exists.
[index](../../index.md)

### crates/engine-core/src/util/mod.rs

```rust
pub use avail::Avail;
pub use storage::Storage;
pub fn get_chunk_size(num_items: usize) -> usize
```
