# engine-core::util::storage

Generated by `make api` — signatures only. Read the source of the
one item you are changing, not the module to find out what exists.
[index](../../index.md)

### crates/engine-core/src/util/storage.rs

```rust
pub struct Storage<T>
  pub data: SegVec<Option<T>>, pub avail: Avail
impl Storage<T>
  pub fn new() -> Self
  pub fn len(&self) -> usize
  pub fn insert(&mut self, v: T) -> u32
  pub fn remove(&mut self, i: u32) -> Option<T>
  pub fn get(&self, i: u32) -> Option<&T>
  pub fn get_mut(&mut self, i: u32) -> Option<&mut T>
```
