# engine-core::util::container

Generated by `make api` — signatures only. Read the source of the
one item you are changing, not the module to find out what exists.
[index](../../index.md)

### crates/engine-core/src/util/container.rs

```rust
impl SubContainer<T>
  pub fn new() -> Self
  pub fn len(&self) -> usize
  pub fn insert(&mut self, item: T) -> (u32, bool)
  pub fn remove(&mut self, i: u32) -> Option<T>
pub struct Container<T>
impl Container<T>
  pub fn new(num_threads: usize) -> Self
  pub fn commit(&mut self)
  pub fn insert(&mut self, item: T) -> u32
  pub fn remove(&mut self, i: u32) -> Option<T>
  pub fn for_each<F>(&mut self, f: F) where F: Fn(&mut T) + Send + Sync,
```
