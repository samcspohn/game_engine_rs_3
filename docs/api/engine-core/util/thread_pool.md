# engine-core::util::thread_pool

Generated by `make api` — signatures only. Read the source of the
one item you are changing, not the module to find out what exists.
[index](../../index.md)

### crates/engine-core/src/util/thread_pool.rs

```rust
impl CachePadded<T>
impl std::ops::Deref for CachePadded<T>
impl WorkerTs
impl Cursor
pub struct ThreadPool
pub struct PoolConfig
  pub num_workers: usize
pub fn init_global<F>(cfg: PoolConfig, on_worker_start: F) where F: Fn(usize) + Send + Sync + 'static,
pub fn global() -> &'static ThreadPool
pub fn is_initialised() -> bool
pub struct DispatchTiming
  pub n_tasks: usize, pub active_workers: usize, pub dispatch_ns: u64, pub main_work_ns: u64, pub barrier_ns: u64, pub worker_wake_min_ns: u64, pub worker_wake_avg_ns: u64, pub worker_wake_max_ns: u64, pub worker_work_min_ns: u64, pub worker_work_avg_ns: u64, pub worker_work_max_ns: u64
pub struct BitmapTaskLayout
  pub words_per_task: usize, pub n_tasks: usize
impl BitmapTaskLayout
pub const fn entities_per_task(self) -> usize
pub fn bitmap_task_layout(n_words: usize) -> BitmapTaskLayout
impl ThreadPool
  pub fn num_threads(&self) -> usize
  pub fn num_workers(&self) -> usize
  pub fn verify_enabled(&self) -> bool
  pub fn parallel_for<F>(&self, n_tasks: usize, f: F) -> DispatchTiming where F: Fn(usize) + Sync,
  pub fn set_current_thread_affinity_mask(cpus: &[usize]) -> bool
  pub fn set_current_thread_affinity_mask(_cpus: &[usize]) -> bool
  pub fn for_chunks<F>(n: usize, chunk_len: usize, f: F) where F: Fn(usize, usize) + Sync,
```
