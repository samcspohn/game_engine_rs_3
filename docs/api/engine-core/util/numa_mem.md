# engine-core::util::numa_mem

Generated by `make api` — signatures only. Read the source of the
one item you are changing, not the module to find out what exists.
[index](../../index.md)

### crates/engine-core/src/util/numa_mem.rs

```rust
pub fn page_size() -> usize
pub fn mbind_to_node(addr: *mut u8, len: usize, node: u32) -> io::Result<usize>
pub fn page_residency(addr: *const u8, len: usize) -> io::Result<Vec<i32>>
pub fn verify_residency_single_node(addr: *const u8, len: usize, expected_node: u32) -> io::Result<(usize, usize)>
pub fn mbind_policy_to_node(addr: *mut u8, len: usize, node: u32) -> io::Result<usize>
pub struct MempolicyGuard
impl MempolicyGuard
  pub fn bind_to_node(node: u32) -> io::Result<Self>
impl Drop for MempolicyGuard
pub struct MempolicyGuard;
impl MempolicyGuard
  pub fn bind_to_node(_node: u32) -> io::Result<Self>
  pub fn mbind_to_node(_addr: *mut u8, _len: usize, _node: u32) -> io::Result<usize>
  pub fn verify_residency_single_node(_addr: *const u8, _len: usize, _expected_node: u32) -> io::Result<(usize, usize)>
```
