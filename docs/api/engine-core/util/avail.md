# engine-core::util::avail

Generated by `make api` — signatures only. Read the source of the
one item you are changing, not the module to find out what exists.
[index](../../index.md)

### crates/engine-core/src/util/avail.rs

```rust
pub struct Avail
  pub data: DaryHeap<Reverse<u32>, 4>
impl Avail
  pub fn new() -> Self
  pub fn commit(&mut self)
  pub fn push(&self, i: u32)
  pub fn pop(&mut self) -> Option<u32>
  pub fn len(&self) -> usize
```
