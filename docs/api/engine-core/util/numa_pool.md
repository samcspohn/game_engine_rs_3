# engine-core::util::numa_pool

Generated by `make api` — signatures only. Read the source of the
one item you are changing, not the module to find out what exists.
[index](../../index.md)

### crates/engine-core/src/util/numa_pool.rs

```rust
impl Chunk
impl R, R> StackJob<F, R>
impl HeapJob<F>
impl Drop for SlotEngagement<'a>
impl TaskSlot
impl Inner
impl Ctx
pub struct Config
  pub topology: Option<Vec<Vec<usize>>>, pub threads: Option<usize>, pub pin: bool
impl Default for Config
pub struct ThreadPool
impl ThreadPool
  pub fn new() -> Self
  pub fn with_threads(n: usize) -> Self
  pub fn with_config(cfg: Config) -> Self
  pub fn num_threads(&self) -> usize
  pub fn run<R: Send>(&self, f: impl FnOnce() -> R + Send) -> R
  pub fn join<A, B, RA, RB>(&self, a: A, b: B) -> (RA, RB) where A: FnOnce() -> RA + Send, B: FnOnce() -> RB + Send, RA: Send, RB: Send,
  pub fn parallel_for<F>(&self, range: Range<usize>, body: F) where F: Fn(Range<usize>) + Sync + Send,
  pub fn parallel_for_static<F>(&self, range: Range<usize>, body: F) where F: Fn(Range<usize>) + Sync + Send,
  pub fn parallel_for_grain<F>(&self, range: Range<usize>, grain: u32, body: F) where F: Fn(Range<usize>) + Sync + Send,
  pub fn scope<'s, R: Send>(&self, f: impl FnOnce(&Scope<'s>) -> R + Send) -> R
  pub fn spawn_background(&self, node: usize, f: impl FnOnce() + Send + 'static)
impl Drop for ThreadPool
pub struct Scope<'s>
impl Scope<'s>
  pub fn spawn<F: FnOnce(&Scope<'s>) + Send + 's>(&self, f: F)
  pub fn pool() -> &'static ThreadPool
  pub fn init(cfg: Config) -> bool
  pub fn init_threads(n: usize) -> bool
  pub fn is_initialized() -> bool
  pub fn run<R: Send>(f: impl FnOnce() -> R + Send) -> R
  pub fn join<A, B, RA, RB>(a: A, b: B) -> (RA, RB) where A: FnOnce() -> RA + Send, B: FnOnce() -> RB + Send, RA: Send, RB: Send,
  pub fn parallel_for<F>(range: Range<usize>, body: F) where F: Fn(Range<usize>) + Sync + Send,
  pub fn parallel_for_grain<F>(range: Range<usize>, grain: u32, body: F) where F: Fn(Range<usize>) + Sync + Send,
  pub fn scope<'s, R: Send>(f: impl FnOnce(&Scope<'s>) -> R + Send) -> R
  pub fn spawn_background(node: usize, f: impl FnOnce() + Send + 'static)
pub use global::{join, parallel_for, parallel_for_grain, run, scope, spawn_background};
```
