# engine-core::mesh

Generated by `make api` — signatures only. Read the source of the
one item you are changing, not the module to find out what exists.
[index](../index.md)

### crates/engine-core/src/mesh/mod.rs

```rust
pub struct Vertex
  pub position: Vec3, pub normal: Vec3, pub uv: Vec2, pub tangent: Vec4
impl Vertex
  pub fn new(position: [f32; 3], normal: [f32; 3], uv: [f32; 2]) -> Self
pub struct Mesh
  pub vertices: Vec<Vertex>, pub indices: Vec<u32>
impl Mesh
  pub fn new(vertices: Vec<Vertex>, indices: Vec<u32>) -> Self
  pub fn triangle_count(&self) -> usize
  pub fn generate_tangents(&mut self)
  pub fn aabb(&self) -> Option<Aabb>
pub struct Aabb
  pub min: Vec3, pub max: Vec3
impl Aabb
  pub fn center(&self) -> Vec3
  pub fn extent(&self) -> Vec3
  pub fn half_extent(&self) -> Vec3
```

### crates/engine-core/src/mesh/primitives.rs

```rust
pub fn cube() -> Mesh
```
