# engine-core::project

Generated by `make api` — signatures only. Read the source of the
one item you are changing, not the module to find out what exists.
[index](../index.md)

### crates/engine-core/src/project.rs

```rust
pub const MANIFEST: &str = "game.json";
pub const PROJECT: &str = "project.json";
pub struct Settings
  pub name: Option<String>, pub startup_scene: Option<PathBuf>
pub fn settings() -> &'static Settings
pub fn enter(fallback: impl Into<PathBuf>) -> std::io::Result<PathBuf>
pub fn pin(path: &str) -> PathBuf
```
