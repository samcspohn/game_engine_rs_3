# engine-core::component

Generated by `make api` — signatures only. Read the source of the
one item you are changing, not the module to find out what exists.
[index](../index.md)

### crates/engine-core/src/component/mod.rs

```rust
pub use world::{EntityMut, EntityView, World};
pub trait Component: Export
pub struct ComponentStorage<T>
impl ComponentStorage<T>
  pub fn new(has_update: bool) -> Self
  pub fn set(&mut self, t_idx: u32, item: T) -> u32
  pub fn drop(&mut self, idx: u32)
  pub fn get(&self, idx: u32) -> Option<&Mutex<T>>
  pub fn first_index(&self) -> Option<u32>
  pub fn _update(&self, dt: f32, world: &World, bitmap_tasks: BitmapTaskLayout)
impl Drop for ComponentStorage<T>
impl ComponentStorageTrait for ComponentStorage<T>
pub struct ComponentRegistry
impl ComponentRegistry
  pub fn new() -> Self
  pub fn register<T: Component + Clone + Send + Sync + 'static>(&mut self, has_update: bool) -> &mut ComponentStorage<T>
  pub fn get_storage<T: Component + Send + Sync + 'static>(&self) -> Option<&ComponentStorage<T>>
  pub fn get_storage_mut<T: Component + Send + Sync + 'static>(&mut self) -> Option<&mut ComponentStorage<T>>
  pub fn inspect(&self, idx: u32, mut f: impl FnMut(&mut dyn Export))
  pub fn update_all(&self, dt: f32, world: &World, bitmap_tasks: BitmapTaskLayout, perf: &mut Option<HashMap<String, PerfCounter>>)
impl Default for ComponentRegistry
pub struct Entity
  pub id: u32
impl Entity
  pub fn new(id: u32) -> Self
```

### crates/engine-core/src/component/world.rs

```rust
pub struct World
impl World
  pub fn handle(&self) -> WorldHandle
  pub fn id(&self) -> WorldId
  pub fn simulating(&self) -> bool
  pub fn set_simulating(&mut self, simulating: bool)
  pub fn hierarchy(&self) -> &TransformHierarchy
  pub fn registry(&self) -> &ComponentRegistry
  pub fn profile(&self, on: bool)
  pub fn entity(&self, entity: Entity) -> EntityView<'_>
  pub fn spawn(&self, t: _Transform, build: impl FnOnce(EntityMut) + Send + 'static)
  pub fn duplicate(&self, src: EntityView, build: impl FnOnce(EntityMut) + Send + 'static)
  pub fn edit(&self, entity: Entity, build: impl FnOnce(EntityMut) + Send + 'static)
  pub fn destroy(&self, entity: Entity)
  pub fn new_entity(&mut self, t: _Transform) -> Entity
  pub fn add_component<T>(&mut self, entity: Entity, mut component: T) where T: Component + Clone + Send + Sync + 'static,
  pub fn remove_component<T>(&mut self, entity: Entity) where T: Component + Clone + Send + Sync + 'static,
  pub fn remove_entity(&mut self, entity: Entity)
  pub fn get_component<T>(&self, entity: Entity) -> Option<&Mutex<T>> where T: Component + Send + Sync + 'static,
  pub fn duplicate_world(&self, simulating: bool) -> WorldHandle
pub struct EntityView<'a>
impl EntityView<'a>
  pub fn id(&self) -> Entity
  pub fn world(&self) -> &'a World
  pub fn transform(&self) -> Transform<'a>
  pub fn get_component<T>(&self, f: impl FnOnce(&mut T)) -> bool where T: Component + Send + Sync + 'static,
  pub fn inspect(&self, f: impl FnMut(&mut dyn Export))
pub struct EntityMut<'a>
impl EntityMut<'_>
  pub fn id(&self) -> Entity
  pub fn world(&self) -> &World
  pub fn transform(&self) -> Transform<'_>
  pub fn add_component<T>(&mut self, component: T) -> &mut Self where T: Component + Clone + Send + Sync + 'static,
  pub fn get_component<T>(&self, f: impl FnOnce(&mut T)) -> bool where T: Component + Send + Sync + 'static,
  pub fn spawn_child(&self, t: _Transform, build: impl FnOnce(EntityMut) + Send + 'static)
```
