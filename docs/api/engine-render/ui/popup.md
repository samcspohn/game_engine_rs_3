# engine-render::ui::popup

Generated by `make api` — signatures only. Read the source of the
one item you are changing, not the module to find out what exists.
[index](../../index.md)

### crates/engine-render/src/ui/popup.rs

```rust
pub struct PopupStyle
  pub fill: u32, pub border: u32, pub radius: f32, pub padding: f32
impl From<Theme> for PopupStyle
impl Default for PopupStyle
pub struct MenuStyle
  pub popup: PopupStyle, pub hover: u32, pub text: u32, pub text_px: f32, pub item_pad: f32
impl From<Theme> for MenuStyle
impl Default for MenuStyle
impl UiCore
  pub fn popup(&mut self, at: [f32; 2], style: PopupStyle) -> Popup
  pub fn context_menu<T: Any + Send>(&mut self, at: [f32; 2], items: &[&str], payload: T, style: MenuStyle) -> Menu
  pub fn menu_choice<T: Any>(&self) -> Option<(usize, &T)>
  pub fn menu_payload<T: Any>(&self) -> Option<&T>
  pub fn popup_open(&self) -> bool
  pub fn close_popup(&mut self)
```
