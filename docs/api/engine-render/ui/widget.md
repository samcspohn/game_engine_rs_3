# engine-render::ui::widget

Generated by `make api` — signatures only. Read the source of the
one item you are changing, not the module to find out what exists.
[index](../../index.md)

### crates/engine-render/src/ui/widget.rs

```rust
pub fn node(self) -> NodeId
impl From<$name> for NodeId
handle! { Label }
handle! { Button }
impl Label
  pub fn set_text(self, ui: &mut UiCore, text: &str)
  pub fn set_color(self, ui: &mut UiCore, color: u32)
handle! { Popup }
handle! { Menu }
handle! { Checkbox }
handle! { Slider }
handle! { TextField }
handle! { RadioGroup }
handle! { Tabs }
handle! { Scrollbar }
impl Checkbox
  pub fn checked(self, ui: &UiCore) -> bool
  pub fn set_checked(self, ui: &mut UiCore, checked: bool)
impl Slider
  pub fn value(self, ui: &UiCore) -> f32
  pub fn set_value(self, ui: &mut UiCore, value: f32)
impl RadioGroup
  pub fn selected(self, ui: &UiCore) -> usize
  pub fn set_selected(self, ui: &mut UiCore, i: usize)
impl Tabs
  pub fn selected(self, ui: &UiCore) -> usize
  pub fn pane(self, ui: &UiCore, i: usize) -> NodeId
  pub fn set_selected(self, ui: &mut UiCore, i: usize)
pub struct StateStyle
  pub idle: UiStyle, pub hover: UiStyle, pub held: UiStyle
impl StateStyle
  pub fn fills(base: UiStyle, idle: u32, hover: u32, held: u32) -> Self
pub struct ButtonStyle
  pub idle: u32, pub hover: u32, pub held: u32, pub text: u32, pub text_px: f32, pub padding: f32, pub radius: f32
impl From<Theme> for ButtonStyle
impl Default for ButtonStyle
pub struct CheckboxStyle
  pub idle: u32, pub hover: u32, pub held: u32, pub text: u32, pub mark: u32, pub box_fill: u32, pub box_border: u32, pub box_px: f32, pub gap: f32, pub text_px: f32, pub padding: f32, pub radius: f32
impl From<Theme> for CheckboxStyle
impl Default for CheckboxStyle
pub struct SliderStyle
  pub track: u32, pub fill: u32, pub thumb: u32, pub width: f32, pub track_px: f32, pub thumb_px: f32
impl From<Theme> for SliderStyle
impl Default for SliderStyle
pub struct RadioStyle
  pub idle: u32, pub hover: u32, pub held: u32, pub text: u32, pub mark: u32, pub circle_fill: u32, pub circle_border: u32, pub circle_px: f32, pub dot_px: f32, pub gap: f32, pub row_gap: f32, pub text_px: f32, pub padding: f32, pub radius: f32
impl From<Theme> for RadioStyle
impl Default for RadioStyle
pub struct TabStyle
  pub idle: u32, pub hover: u32, pub held: u32, pub selected: u32, pub text: u32, pub text_dim: u32, pub text_px: f32, pub gap: f32, pub padding: f32, pub radius: f32
impl TabStyle
impl From<Theme> for TabStyle
impl Default for TabStyle
pub struct ScrollbarStyle
  pub track: u32, pub thumb: u32, pub thumb_hover: u32, pub width: f32, pub min_thumb_px: f32, pub radius: f32
impl From<Theme> for ScrollbarStyle
impl Default for ScrollbarStyle
impl UiCore
  pub fn set_state_style(&mut self, n: impl Into<NodeId>, style: StateStyle)
  pub fn button(&mut self, parent: impl Into<NodeId>, text: &str, style: ButtonStyle) -> Button
  pub fn checkbox(&mut self, parent: impl Into<NodeId>, text: &str, style: CheckboxStyle) -> Checkbox
  pub fn slider(&mut self, parent: impl Into<NodeId>, style: SliderStyle) -> Slider
  pub fn radio_group(&mut self, parent: impl Into<NodeId>, options: &[&str], style: RadioStyle) -> RadioGroup
  pub fn tabs(&mut self, parent: impl Into<NodeId>, labels: &[&str], style: TabStyle) -> Tabs
  pub fn scrollbar(&mut self, parent: impl Into<NodeId>, area: impl Into<NodeId>, style: ScrollbarStyle) -> Scrollbar
```
