# engine-render::ui::text_field

Generated by `make api` — signatures only. Read the source of the
one item you are changing, not the module to find out what exists.
[index](../../index.md)

### crates/engine-render/src/ui/text_field.rs

```rust
pub struct TextFieldStyle
  pub fill: u32, pub border: u32, pub border_focus: u32, pub text: u32, pub hint: u32, pub caret: u32, pub selection: u32, pub width: f32, pub text_px: f32, pub padding: f32, pub radius: f32
impl From<Theme> for TextFieldStyle
impl Default for TextFieldStyle
impl FieldState
impl TextField
  pub fn text(self, ui: &UiCore) -> &str
  pub fn set_text(self, ui: &mut UiCore, text: &str)
  pub fn set_hint(self, ui: &mut UiCore, hint: &str)
  pub fn submitted(self, ui: &UiCore) -> bool
  pub fn changed(self, ui: &UiCore) -> bool
  pub fn focus(self, ui: &mut UiCore)
  pub fn cursor(self, ui: &UiCore) -> usize
impl UiCore
  pub fn text_field(&mut self, parent: impl Into<NodeId>, text: &str, style: TextFieldStyle) -> TextField
  pub fn field_text(&self, n: impl Into<NodeId>) -> Option<&str>
```
