# engine-render::ui::theme

Generated by `make api` — signatures only. Read the source of the
one item you are changing, not the module to find out what exists.
[index](../../index.md)

### crates/engine-render/src/ui/theme.rs

```rust
pub struct Theme
  pub backdrop: u32, pub panel: u32, pub control: u32, pub control_hover: u32, pub control_held: u32, pub selection: u32, pub text: u32, pub text_dim: u32, pub text_strong: u32, pub accent: u32, pub outline: u32, pub text_px: f32, pub radius: f32, pub pad: f32
impl Theme
pub const DARK: Theme = Theme
impl Default for Theme
pub fn theme() -> Theme
pub fn set_theme(t: Theme)
```
