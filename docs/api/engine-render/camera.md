# engine-render::camera

Generated by `make api` — signatures only. Read the source of the
one item you are changing, not the module to find out what exists.
[index](../index.md)

### crates/engine-render/src/camera.rs

```rust
pub const CAMERA_COLOR_FORMAT: Format = Format::R16G16B16A16_SFLOAT;
pub const CAMERA_DEPTH_FORMAT: Format = Format::D32_SFLOAT;
pub enum CameraResolution
  MatchSwapchain, Fixed([u32; 2])
impl CameraResolution
  pub fn depends_on_swapchain(&self) -> bool
  pub fn blits_to_swapchain(&self) -> bool
pub const MAX_CAMERAS: usize = 8;
impl Default for Projection
impl Projection
pub struct CameraState
pub struct CameraHandle(Arc<CameraState>);
impl CameraHandle
  pub fn new(world: WorldId) -> Self
  pub fn is(&self, other: &CameraHandle) -> bool
  pub fn is_orphan(&self) -> bool
  pub fn worlds(&self) -> Vec<WorldId>
  pub fn draw_world(&self, world: WorldId)
  pub fn slot(&self) -> usize
  pub fn view_proj(&self) -> (Mat4, Vec3)
  pub fn set_view_proj(&self, view_proj: Mat4, eye: Vec3)
  pub fn set_from_trs(&self, pos: Vec3, rot: Quat)
  pub fn fov_y(&self) -> f32
  pub fn set_grid(&self, grid: Option<[f32; 4]>)
  pub fn grid(&self) -> Option<[f32; 4]>
  pub fn set_projection(&self, fov_y_radians: f32, z_near: f32, z_far: f32)
  pub fn set_rect(&self, rect: Option<[f32; 4]>)
  pub fn rect(&self) -> Option<[f32; 4]>
  pub fn contains(&self, p: [f32; 2]) -> bool
  pub fn camera_count() -> usize
  pub fn of_world(world: WorldId) -> Option<CameraHandle>
pub struct DrawPlan
  pub commands: Vec<DrawIndexedIndirectCommand>, pub total_renderers: u32
pub struct WorldSource<'a>
  pub id: WorldId, pub transforms: &'a WorldTransformGpu, pub renderers: &'a GpuRenderers, pub plan: DrawPlan
impl WorldSource<'_>
pub struct CameraSceneResources<'a>
  pub cb_allocator: &'a Arc<StandardCommandBufferAllocator>, pub descriptor_set_allocator: &'a Arc<StandardDescriptorSetAllocator>, pub memory_allocator: &'a Arc<StandardMemoryAllocator>, pub pipeline: &'a Arc<GraphicsPipeline>, pub queue_family_index: u32, pub mvp_build_pipeline: &'a Arc<ComputePipeline>, pub mesh_store: &'a GpuMeshStore, pub texture_store: &'a GpuTextureStore, pub material_store: &'a GpuMaterialStore, pub mvp_build_pass2_pipeline: &'a Arc<ComputePipeline>, pub cull_pass2_args_pipeline: &'a Arc<ComputePipeline>, pub draw_compact_pipeline: &'a Arc<ComputePipeline>, pub hiz_reduce_depth_pipeline: &'a Arc<ComputePipeline>, pub hiz_reduce_mip_pipeline: &'a Arc<ComputePipeline>, pub hiz_reduce_mip2_pipeline: &'a Arc<ComputePipeline>
impl DrawResources
impl WorldDraw
pub struct RenderCamera
impl RenderCamera
  pub fn new_match_swapchain(state: CameraHandle, swapchain_extent: [u32; 2], scene: &CameraSceneResources<'_>, worlds: &[WorldSource<'_>]) -> Self
  pub fn new(state: CameraHandle, resolution: CameraResolution, swapchain_extent: [u32; 2], scene: &CameraSceneResources<'_>, worlds: &[WorldSource<'_>]) -> Self
  pub fn on_swapchain_resize(&mut self, new_swapchain_extent: [u32; 2], scene: &CameraSceneResources<'_>) -> bool
  pub fn set_resolution(&mut self, resolution: CameraResolution, swapchain_extent: [u32; 2], scene: &CameraSceneResources<'_>) -> bool
  pub fn ensure_current(&mut self, scene: &CameraSceneResources<'_>, worlds: &[WorldSource<'_>]) -> bool
  pub fn needs_structural_rebuild(&self, worlds: &[WorldSource<'_>], force: bool) -> bool
  pub fn write_template_bases(&self, worlds: &[WorldSource<'_>])
  pub fn write_cull_view_proj(&self, live_view_proj: [f32; 16])
  pub fn set_cull_lock(&mut self, locked: bool, live_view_proj: [f32; 16])
  pub fn cull_lock(&self) -> bool
  pub fn apply_pending_hiz_freeze(&mut self) -> bool
  pub fn hiz_frozen(&self) -> bool
  pub fn set_occlusion_enabled(&mut self, enabled: bool, scene: &CameraSceneResources<'_>) -> bool
  pub fn occlusion_enabled(&self) -> bool
  pub fn state(&self) -> &CameraHandle
  pub fn extent(&self) -> [u32; 2]
  pub fn aspect(&self) -> f32
  pub fn resolution(&self) -> CameraResolution
  pub fn color_image(&self) -> &Arc<Image>
  pub fn depth_image(&self) -> &Arc<Image>
  pub fn color_view(&self) -> &Arc<ImageView>
  pub fn depth_view(&self) -> &Arc<ImageView>
  pub fn scene_secondaries_pass1(&self) -> impl Iterator<Item = &Arc<SecondaryAutoCommandBuffer>>
  pub fn scene_secondaries_pass2(&self) -> impl Iterator<Item = &Arc<SecondaryAutoCommandBuffer>>
  pub fn overlay_secondary(&self, slot: usize) -> &Arc<SecondaryAutoCommandBuffer>
  pub fn write_overlay(&self, view_proj: Mat4, eye: Vec3)
  pub fn cull_secondary(&self) -> &Arc<SecondaryAutoCommandBuffer>
  pub fn hiz_build_secondary(&self) -> &Arc<SecondaryAutoCommandBuffer>
  pub fn cull_pass2_secondary(&self) -> &Arc<SecondaryAutoCommandBuffer>
  pub fn history_update_secondary(&self) -> &Arc<SecondaryAutoCommandBuffer>
  pub fn view_proj_buf(&self) -> &Subbuffer<[[f32; 16]]>
  pub fn view_proj_staging_buf(&self, slot: usize) -> &Subbuffer<[[f32; 16]]>
  pub fn write_view_proj(&self, view_proj: Mat4, eye: Vec3)
  pub fn cull_view_proj_buf(&self) -> &Subbuffer<[[f32; 16]]>
  pub fn cull_view_proj_staging_buf(&self, slot: usize) -> &Subbuffer<[[f32; 16]]>
  pub fn advance_staging_slot(&mut self)
```
