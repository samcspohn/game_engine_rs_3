# engine-render::overlay

Generated by `make api` — signatures only. Read the source of the
one item you are changing, not the module to find out what exists.
[index](../index.md)

### crates/engine-render/src/overlay.rs

```rust
pub struct OverlayVertex
  pub position: [f32; 3], pub color: [f32; 4]
impl OverlayVertex
  pub fn new(position: glam::Vec3, color: [f32; 4]) -> Self
impl CameraOverlay
```
