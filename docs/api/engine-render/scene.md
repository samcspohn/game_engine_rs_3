# engine-render::scene

Generated by `make api` — signatures only. Read the source of the
one item you are changing, not the module to find out what exists.
[index](../index.md)

### crates/engine-render/src/scene.rs

```rust
pub struct CameraComponent
  pub fov_y_radians: f32, pub z_near: f32, pub z_far: f32
impl CameraComponent
  pub fn new() -> Self
  pub fn camera(&self) -> Option<&CameraHandle>
impl Default for CameraComponent
impl Component for CameraComponent
pub struct OrbitController
  pub target: Vec3, pub yaw: f32, pub pitch: f32, pub distance: f32, pub up: Vec3, pub orbit_sensitivity: f32, // radians per pixel, pub pan_sensitivity: f32, // world units per pixel per unit distance, pub zoom_sensitivity: f32, // multiplicative per scroll line
impl OrbitController
  pub fn new() -> Self
  pub fn for_camera(camera: CameraHandle) -> Self
  pub fn eye(&self) -> Vec3
impl Default for OrbitController
impl Component for OrbitController
```
